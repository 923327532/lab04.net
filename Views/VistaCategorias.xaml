<UserControl x:Class="NeptunoApp.Views.VistaCategorias"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Actualizar" Padding="12,6" Margin="0,0,8,0" Command="{Binding CargarCommand}"/>
            <Button Content="Nuevo" Padding="12,6" Margin="0,0,8,0" Command="{Binding NuevoCommand}"/>
            <Button Content="Guardar" Padding="12,6" Margin="0,0,8,0" Command="{Binding GuardarCommand}"/>
            <Button Content="Eliminar" Padding="12,6" Margin="0,0,8,0" Command="{Binding EliminarCommand}"/>
            <Button Content="Cancelar" Padding="12,6" Command="{Binding CancelarCommand}"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Categorias}" SelectedItem="{Binding Seleccionada}"
                  AutoGenerateColumns="False" IsReadOnly="True" AlternatingRowBackground="GhostWhite"
                  RowHeight="26" BorderBrush="#A0A0A0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Codigo" Binding="{Binding CategoriaID}" Width="80"/>
                <DataGridTextColumn Header="Nombre de Categoria" Binding="{Binding NombreCategoria}" Width="220"/>
                <DataGridTextColumn Header="Descripcion" Binding="{Binding Descripcion}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="2" Margin="0,10,0,0" Padding="10" BorderBrush="#A0A0A0" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Nombre" VerticalAlignment="Center" Margin="4"/>
                <TextBox Grid.Row="0" Grid.Column="1" Margin="4" Text="{Binding Seleccionada.NombreCategoria, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Descripcion" VerticalAlignment="Center" Margin="4"/>
                <TextBox Grid.Row="1" Grid.Column="1" Margin="4" AcceptsReturn="True" MinHeight="60"
                         Text="{Binding Seleccionada.Descripcion, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
